const aName = "peter";

const capitilazation = aName.substring(2, 3);
const firstPart = aName.substring(0, 2);
const lastPart = aName.substring(3);

console.log(firstPart + capitilazation.toUpperCase() + lastPart);
